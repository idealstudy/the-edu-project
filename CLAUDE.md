# CLAUDE.md — mvp-front (placeholder)

이 파일은 placeholder다. mvp-front 코드 본체는 별도 라운드에서 `git subtree add --prefix=mvp-front --squash <commit>` 으로 연결된다. 그때 mvp-front 레포의 원본 `CLAUDE.md` 본문으로 덮어쓴다.

연결 전까지는 프론트엔드 코드 작업은 다음 레포에서 직접 한다:
- https://github.com/idealstudy/mvp-front (public 공개 미러)

루트 `CLAUDE.md` §3 Reading order의 "프론트엔드 코드 작업" 흐름은 subtree 연결 후 활성화된다.
