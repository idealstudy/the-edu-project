# (통합됨) 디자인 시스템 2.0

이 문서(2.0 톤·컴포넌트)의 내용은 **`mvp-front/docs/DESIGN.md`** 로 통합됐다. 정본은 거기 하나다.

- 2.0 리디자인 결정 배경은 `DESIGN.md` **부록 A** 에 이력 보존됨.
- 문서 정본: `mvp-front/docs/DESIGN.md`
- 코드 정본: `mvp-front/src/styles/globals.css` (값 충돌 시 코드가 이김)

파일 이름에 세대(2.0/3.0)를 박으면 정본이 갈린다. 세대 차이는 `DESIGN.md` 안의 절(§1 세대 정리)로만 다룬다.
