# (통합됨) 디자인 시스템

이 문서는 **`mvp-front/docs/DESIGN.md`** 로 통합됐다. 정본은 거기 하나다.

- 문서 정본: `mvp-front/docs/DESIGN.md`
- 코드 정본: `mvp-front/src/styles/globals.css`
- 값이 서로 다르면 `globals.css` 가 이긴다.

여기에 디자인 시스템 내용을 다시 만들지 마라. 옛 경로로 찾아온 사람을 위한 리다이렉트로만 남긴다.
